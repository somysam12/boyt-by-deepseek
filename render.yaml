services:
  - type: web
    name: telegram-key-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python verify_key_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
      - key: PORT
        value: 10000
